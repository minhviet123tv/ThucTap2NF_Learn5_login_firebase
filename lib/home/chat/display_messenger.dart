import 'package:flutter/material.dart';

class DisplayMessenger extends StatelessWidget {
  const DisplayMessenger({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.green,);
  }
}
